@"
# Zula Travels — myapp

Monorepo for Zula Travels — frontend (Vite + React) and backend (Node/Express).

Folders:
- frontend/
- backend/

Run the frontend:
> cd frontend
> npm install
> npm run dev

Run the backend:
> cd backend
> npm install
> node server.js

"@ | Out-File -Encoding UTF8 README.md
"# Zula_Travels" 
